const addIngredient = require('./addIngredient');
const verifyId = require('./verifyId');
const searchRecipe = require('./searchRecipe');
const recipesArray = require('./recipesArray');
const deleteIngredient = require('./deleteIngredient');

module.exports = {
  addIngredient,
  verifyId,
  searchRecipe,
  recipesArray,
  deleteIngredient,
}
